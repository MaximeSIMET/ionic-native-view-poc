Build the capacitor plugin : <br>
> cd capacitor-native-app && npm i && npm run build


Build the ionic app : <br>
> cd ionic-native-view && npm install && npm run build && npx cap sync ios && npx cap open ios 

